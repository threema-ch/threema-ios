/// Import the shared configuration
#include "CustomOnPremConfig.xcconfig"
#include "../Base/NotificationExtensionBaseConfig.xcconfig"

PRODUCT_NAME = $(APP_NAME) NotificationExtension
PRODUCT_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).NotificationExtension
