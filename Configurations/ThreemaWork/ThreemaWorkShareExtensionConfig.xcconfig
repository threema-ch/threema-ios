/// Import the shared configuration
#include "../Base/ShareExtensionBaseConfig.xcconfig"
#include "ThreemaWorkConfig.xcconfig"

PRODUCT_NAME = $(APP_NAME) ShareExtension
PRODUCT_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).ThreemaShareExtension
